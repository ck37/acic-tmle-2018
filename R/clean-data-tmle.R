clean_data_tmle =
  function(covars_df,
           outcome_vec,
           treatment_vec,
           verbose = verbose) {
    
  # TODO: remove constant variables, etc.
    
  results = list(
    covariate_df = covars_df
  )
  return(results)
} 